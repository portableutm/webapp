import React from 'react';
import '../Ades.css';

function RightArea({children}) {
    return (
        <div className='rightArea'>
            {children}
        </div>
    )
}

export default RightArea;